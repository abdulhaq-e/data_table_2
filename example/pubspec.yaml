name: example
description: data_table_2 example

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.3
  data_table_2:
    path: ../
    #git: https://github.com/maxim-saplin/data_table_2

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
